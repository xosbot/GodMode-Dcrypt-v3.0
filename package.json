{
  "name": "godmode-dcrypt-monolith-root",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "start": "npm install --prefix backend && node backend/src/server",
    "build": "echo 'Root build complete'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
